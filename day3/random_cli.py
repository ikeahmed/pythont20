#import random module to use random related functions
import random
random.randint(1,5) # try in cli couple of times
random.randrange(10) # Integer from 0 to 9 inclusive, try in cli few times
random.randrange(0, 101, 2)  #even numbers until 100, try in cli few times
random.random()   #Random float:  0.0 <= x < 1.0